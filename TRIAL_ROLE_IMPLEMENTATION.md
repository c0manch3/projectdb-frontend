# Trial Role Implementation - Frontend

## Обзор
Реализована поддержка новой роли пользователя **Trial** для потенциальных покупателей платформы. Trial пользователи имеют доступ только для чтения ко всем данным, кроме информации о сотрудниках с ролью Employee.

## Дата реализации
2025-12-08

## Изменения в кодовой базе

### 1. Обновление типов и утилит

#### `src/store/types.ts`
- Обновлен тип `UserRole` для включения `'Trial'`:
```typescript
export type UserRole = 'Admin' | 'Manager' | 'Employee' | 'Trial';
```

#### `src/utils/roles.ts`
Добавлены новые утилиты для работы с Trial ролью:
- `isTrial(role)` - проверка, является ли пользователь Trial
- `canModifyData(role)` - проверка прав на изменение данных (Trial = false)
- `canViewEmployees(role)` - проверка прав на просмотр сотрудников (Trial = false)
- `canManageDocuments(role)` - проверка прав на управление документами (Trial = false)

### 2. Новый хук для проверки прав

#### `src/hooks/use_permissions.ts` (новый файл)
Создан универсальный хук `usePermissions()` для проверки прав пользователя:

```typescript
const permissions = usePermissions();
const {
  isTrial,
  canModifyData,
  canCreateProjects,
  canEditProjects,
  canDeleteProjects,
  canViewEmployees,
  canManageDocuments,
  // ... другие проверки
} = permissions;
```

**Дополнительные хуки:**
- `useUserRole()` - получение текущей роли пользователя
- `useIsTrialUser()` - быстрая проверка Trial статуса

### 3. UI компоненты для Trial пользователей

#### `src/components/common/trial_badge/trial_badge.tsx` (новый файл)
Компонент badge для визуальной индикации Trial режима:
- Автоматически отображается в header для Trial пользователей
- Показывает иконку замка 🔒 и текст "Тестовый доступ"
- Имеет анимацию пульсации для привлечения внимания

#### Стили в `src/styles/style.css`
Добавлены новые CSS классы:
- `.trial-badge` - основной badge компонент
- `.trial-access-message` - сообщения об ограничениях
- `.trial-access-message--compact` - компактная версия для таблиц
- `.button--trial-disabled` - стили для отключенных кнопок
- Анимации и hover эффекты

### 4. Обновление компонентов

#### `src/components/layout/header/header.tsx`
- Добавлен импорт `TrialBadge` компонента
- Badge отображается рядом с информацией о пользователе
- Обновлена функция `getUserRoleDisplayName()` для Trial роли
- Обновлена функция `hasAccessToNavItem()`:
  - Trial может видеть: Projects, Companies, Workload
  - Trial НЕ может видеть: Employees

#### `src/pages/projects/projects.tsx`
- Заменены явные проверки ролей на использование хука `usePermissions()`
- Кнопки создания/редактирования/удаления скрыты для Trial пользователей
- Trial пользователи могут только просматривать проекты

#### `src/pages/employees/employees.tsx`
- Добавлен хук `usePermissions()`
- Создана фильтрация `displayUsers` для скрытия Employee от Trial:
```typescript
const displayUsers = isTrial
  ? filteredUsers.filter(user => user.role !== 'Employee')
  : filteredUsers;
```
- Обновлена функция `getRoleDisplayName()` для Trial роли

#### `src/pages/project_detail/project_detail.tsx`
- Добавлен хук `usePermissions()`
- Кнопки загрузки документов скрыты для Trial (`canManageDocuments`)
- Кнопки скачивания/удаления документов скрыты для Trial
- Добавлено компактное сообщение "Просмотр документов недоступен в тестовом режиме" для Trial пользователей

### 5. API обработка ошибок

#### `src/services/api.ts`
Добавлен interceptor для обработки 403 ошибок:
- Определяет, является ли пользователь Trial
- Показывает дружелюбное сообщение: "Это действие недоступно в тестовом режиме"
- Для не-Trial пользователей показывает: "У вас нет прав для выполнения этого действия"

```typescript
if (error.response?.status === 403) {
  const currentUser = state.auth.user;
  if (currentUser?.role === 'Trial') {
    toast.error('Это действие недоступно в тестовом режиме');
  } else {
    toast.error('У вас нет прав для выполнения этого действия');
  }
}
```

## Матрица доступа Trial пользователя

### ✅ Разрешено (READ-ONLY):
- Просмотр списка проектов
- Просмотр деталей проекта
- Просмотр списка компаний (Customer, Contractor, но не Employee companies)
- Просмотр деталей компаний
- Просмотр workload (загруженность сотрудников)
- Просмотр аналитики
- Просмотр графиков платежей
- Просмотр метаданных документов (имена, типы, даты)

### ❌ Запрещено:
- Создание/редактирование/удаление проектов
- Создание/редактирование/удаление компаний
- Загрузка документов
- Скачивание документов
- Удаление документов
- Замена документов
- Просмотр списка сотрудников с ролью Employee
- Просмотр профилей Employee
- Создание/редактирование/удаление workload
- Создание/редактирование/удаление графиков платежей
- Любые POST/PUT/PATCH/DELETE запросы

## Навигация для Trial пользователей

Trial пользователи видят в header следующие разделы:
- **Проекты** ✅
- **Компании** ✅
- **Загруженность** ✅
- ~~Сотрудники~~ ❌ (скрыт)

## Тестирование

### Рекомендуемые тест-кейсы:

1. **Визуальная индикация:**
   - [ ] Trial badge отображается в header
   - [ ] Badge имеет оранжевый цвет и иконку замка
   - [ ] Роль пользователя отображается как "Тестовый доступ"

2. **Навигация:**
   - [ ] Trial видит только Projects, Companies, Workload в меню
   - [ ] Trial НЕ видит раздел Employees
   - [ ] Прямой переход по URL /employees блокируется

3. **Проекты:**
   - [ ] Trial может открыть список проектов
   - [ ] Trial НЕ видит кнопку "Создать проект"
   - [ ] Trial НЕ видит кнопки редактирования/удаления в таблице
   - [ ] Trial может открыть детали проекта (read-only)

4. **Документы:**
   - [ ] Trial видит список документов (metadata)
   - [ ] Trial НЕ видит кнопку "Загрузить документ"
   - [ ] Trial НЕ видит кнопки "Скачать" и "Удалить" у документов
   - [ ] Отображается сообщение "Просмотр документов недоступен в тестовом режиме"

5. **Сотрудники:**
   - [ ] Trial НЕ видит в списке пользователей тех, кто имеет роль Employee
   - [ ] Trial видит Admin, Manager, Customer, Trial в списке

6. **API ошибки:**
   - [ ] При попытке POST/PUT/DELETE запроса Trial получает 403
   - [ ] Отображается toast: "Это действие недоступно в тестовом режиме"

## Файлы для review

### Новые файлы:
- `src/hooks/use_permissions.ts`
- `src/components/common/trial_badge/trial_badge.tsx`
- `TRIAL_ROLE_IMPLEMENTATION.md`

### Измененные файлы:
- `src/store/types.ts`
- `src/utils/roles.ts`
- `src/services/api.ts`
- `src/components/layout/header/header.tsx`
- `src/pages/projects/projects.tsx`
- `src/pages/employees/employees.tsx`
- `src/pages/project_detail/project_detail.tsx`
- `src/styles/style.css`

## Дальнейшие улучшения (опционально)

1. **Аналитика использования:**
   - Отслеживание действий Trial пользователей
   - Статистика попыток выполнить запрещенные действия

2. **Ограничение по времени:**
   - Добавить таймер срока действия Trial доступа
   - Countdown в UI

3. **Конверсия:**
   - Кнопка "Купить полную версию"
   - Форма "Связаться с продажами"
   - Watermark на страницах

4. **Feedback:**
   - Форма обратной связи для Trial пользователей
   - Вопросы об опыте использования

5. **Модальные окна:**
   - Обновить все modal компоненты для поддержки read-only режима
   - Добавить проверки permissions в модальных окнах

## Примечания

- Все изменения совместимы с существующими ролями (Admin, Manager, Employee)
- Не требуется миграция данных
- Trial роль полностью блокируется на бэкенде с помощью guards
- Фронтенд только скрывает UI элементы для лучшего UX
- Безопасность обеспечивается на уровне API, а не только UI

## Backend интеграция

Фронтенд готов к интеграции с бэкендом, который использует:
- `NotTrialGuard` - блокирует Trial на записывающих эндпоинтах
- `AppManagerOrTrialGuard` - разрешает чтение для Trial
- Возвращает 403 ошибки с сообщением "Trial users cannot perform this action"

## Контакты для вопросов

При возникновении вопросов по реализации Trial роли обращайтесь к команде разработки.
